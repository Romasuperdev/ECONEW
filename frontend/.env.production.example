# Modèle de configuration front pour la production.
# Copier vers .env.production (utilisé automatiquement par `npm run build`).
# Mettre l'URL réelle de l'API (sans /api final, il est ajouté automatiquement).
VITE_API_URL=https://api.votre-domaine.com
